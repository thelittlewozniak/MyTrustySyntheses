@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor

@{
    if (HttpContextAccessor.HttpContext.Session.GetString("??") != null)
    {
        <!--Html.Partial("NavbarUser") -->
        @await Html.PartialAsync("~/Views/Shared/NavbarUser.cshtml")
    }
    else
    {
        <!-- Html.Partial("NavbarDefault") -->
        @await Html.PartialAsync("~/Views/Shared/NavbarDefault.cshtml")
    }
}